# Moving MNIST 验证：基于深度卡尔曼滤波 (DKF) 的运动预测

## 1. 验证目标
在将 DKF 框架应用于复杂的真实大气数据之前，本阶段旨在 **Moving MNIST** 数据集上验证核心架构的正确性与预测能力。

**核心问题**：
- DKF 的隐状态 $Z_t$ 是否能有效编码运动信息（位置、速度）？
- 模型能否准确预测数字的未来运动轨迹？
- 预测帧是否能保持数字的形状锐度，而非模糊一团？

**技术栈**：PyTorch (核心框架), torchvision (数据读取), NumPy。

---

## 2. 数据流水线 (`dataset.py`)

* **数据集**：`torchvision.datasets.MovingMNIST` 或自定义生成脚本。
* **观测张量 $X_t$ 构成 (Channels = 1, 空间维度 $64 \times 64$)**：
  * 单通道灰度图像，包含 1~3 个运动中的手写数字。
* **预处理策略**：
  * 滑动窗口切片：划分历史观测窗口 $T_{past}$ 和未来预测窗口 $T_{future}$。
  * 归一化：将像素值从 $[0, 255]$ 线性缩放至 $[0, 1]$ 或 $[-1, 1]$。

---

## 3. 网络架构 (`modules.py`)

系统遵循隐马尔可夫结构，包含推断网络与生成网络。为保留图像的空间拓扑结构，所有核心序列模块使用 **ConvLSTM** 或 **ConvGRU**。

### 3.1 识别网络 / 后验推断 (Recognition Network, $q_\phi$)
在训练阶段充当“平滑器/滤波器”，从真实观测序列中提取纯净的隐状态轨迹。
* **架构选择**：采用严格因果的 **q-RNN** 结构，推断 $q(z_t | x_1, \dots, x_t)$。
* **输入**：当前时刻降维特征 $e_t$ (由独立 CNN 编码器提取自 $X_t$) 与上一时刻隐状态 $Z_{t-1}$。
* **输出**：当前后验高斯分布的均值 $\mu_{qt}$ 和对数方差 $\log\Sigma_{qt}$。
* **重参数化采样**：实现可导采样函数 $\hat{Z}_t = \mu_{qt} + \epsilon \odot \sqrt{\exp(\log\Sigma_{qt})}$，其中 $\epsilon \sim \mathcal{N}(0, I)$。

### 3.2 生成模型 / 动力学与观测 (Generative Model, $p_\theta$)
* **A. 状态转移网络 (Prior / Transition, $p_\theta(z_t | z_{t-1})$)**
  * **作用**：基于 $t-1$ 时刻的隐状态自回归推演 $t$ 时刻的先验分布。
  * **输出**：先验均值 $\mu_{pt}$ 和对数方差 $\log\Sigma_{pt}$。
* **B. 观测解码网络 (Emission, $p_\theta(X_t | Z_t)$)**
  * **作用**：将高维隐状态还原为单通道 2D 图像网格。
  * **架构**：堆叠 `ConvTranspose2d` 进行空间上采样。
  * **输出激活**：`Sigmoid`，将像素值约束在 $[0, 1]$。

---

## 4. 变分下界损失函数 (`loss.py`)

优化目标为最大化 ELBO，即最小化以下两项之和：

1. **重构误差 (Reconstruction Loss)**：
   * 计算网络解码出的 $\hat{X}_t$ 与真实数据 $X_t$ 之间的 MSE 或 BCE。
   * 关注图像整体重建质量，无需特殊加权。

2. **KL 散度 (KL Divergence)**：
   * 计算后验推断分布 $\mathcal{N}(\mu_{qt}, \Sigma_{qt})$ 与先验转移分布 $\mathcal{N}(\mu_{pt}, \Sigma_{pt})$ 之间的散度。
   * **严格约束**：严禁使用蒙特卡洛采样计算！必须使用两个对角高斯分布之间的闭式解析解公式（Closed-form analytical solution）进行纯张量代数运算。

---

## 5. 运行模式与执行流 (`model.py`)

`DeepKalmanFilter` 主类的 `forward` 函数必须实现两种截然不同的控制流：

* **训练流 (Filtering & Alignment)**：开启推断网络 $q_\phi$。每一时间步利用真实观测提取后验状态 $\hat{Z}_t$，同时运行转移网络 $p_\theta$ 获取先验分布。收集全序列的重构误差与 KL 散度进行反向传播。

* **预测流 (Autoregressive Forecasting)**：在输入前 $T_{past}$ 个观测完成初始状态估计后，进入滚动预测模式。
  * 在每个时间步，利用真实观测 $X_t$ 和上一时刻隐状态 $Z_{t-1}$，通过推断网络 $q_\phi$ 校准当前隐状态 $Z_t$。
  * 随后利用转移网络 $p_\theta$ 向前推演下一步 $Z_{t+1}$。
  * 通过解码器输出预测帧 $\hat{X}_{t+1}$。
  * 此过程自回归向前推进 $T_{future}$ 步，逐帧预测数字的运动轨迹。

---

## 6. 验证指标

* **定性评估**：可视化预测帧与真实帧的对比，观察运动轨迹是否准确、形状是否清晰。
* **定量评估**：
  * 帧级 MSE / PSNR
  * 多步预测的误差累积曲线
* **隐状态分析**（可选）：通过降维可视化（如 t-SNE）观察 $Z_t$ 是否形成与数字位置/速度相关的流形结构。

---

## 7. AI 辅助编程 (Vibe Coding) 实施提示词建议

1. **模块构建**："请根据第 3 节，使用 PyTorch 编写 `modules.py`。首先实现一个支持 2D 空间特征的 `ConvLSTMCell`，然后分别构建编码器、推断网络、状态转移网络和解码器。针对 Moving MNIST 单通道输入，确保各模块之间的 Channel 和 H/W 维度完全对齐。"
2. **主类拼接**："基于刚才的模块，编写 `model.py`。重点实现第 5 节描述的 `forward` 函数，明确区分出含有重参数化采样的训练循环，和滚动预测模式下的预测循环。"
3. **损失函数**："请根据第 4 节编写损失函数。KL 散度部分请查阅标准的高斯分布解析解公式，不要使用采样。直接使用 MSE 作为重构损失即可。"
4. **数据准备**："请编写 `dataset.py`，使用 `torchvision` 加载 Moving MNIST 数据集，并实现滑动窗口切片，生成 $T_{past}$ 历史帧和 $T_{future}$ 预测帧的样本对。"
5. **训练脚本**："请编写 `train.py`，实现完整的训练循环，包含模型初始化、优化器设置、损失计算和日志记录。预留模型保存接口。"
6. **预测可视化**："请编写 `eval.py`，加载训练好的模型，在测试集上进行滚动预测，并将预测结果与真实帧拼接成 GIF 或视频进行可视化对比。"

---

## 8. 迁移至 WeatherBench 的修改清单

若本阶段验证通过，迁移至真实大气数据时需进行以下修改：

| 模块 | 修改内容 |
|------|----------|
| `dataset.py` | 替换为 WeatherBench 的 netCDF4 数据读取；4 通道输入；降水使用 $\log(1+x)$ 变换 |
| `modules.py` (Decoder) | 输出通道从 1 改为 4；降水通道使用 `Softplus` 激活 |
| `loss.py` | 重构损失增加降水通道的动态权重 |
| `model.py` | 无需修改，逻辑通用 |